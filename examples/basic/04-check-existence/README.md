# Check Existence

Demonstrates using `Cache::has()` to check whether a cache item exists and is not expired.

## Run

```bash
php index.php
```

## Expected Output

```
Before set - has 'status': false
After set  - has 'status': true
Has 'other_key': false
```
