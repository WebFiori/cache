# Set and Get

Demonstrates the most basic cache operations: storing a value with `Cache::set()` and retrieving it with `Cache::get()`.

## Run

```bash
php index.php
```

## Expected Output

```
Stored 'greeting' in cache.
Retrieved: Hello, World!
Non-existent key returns: NULL
```
